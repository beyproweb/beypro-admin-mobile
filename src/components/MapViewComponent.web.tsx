// Web-specific export (stub stubs since react-native-maps doesn't work on web)
const MapView = () => null;
const Marker = () => null;
const Polyline = () => null;
const PROVIDER_GOOGLE = null;

export { MapView, Marker, Polyline, PROVIDER_GOOGLE };
export default MapView;
